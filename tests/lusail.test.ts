import { describe, expect, test } from '@jest/globals';
import { Lusail } from '@lusail/index';

describe('Lusail', () => {
  test('is defined', async () => {
    expect(Lusail).toBeDefined();
  });
});
